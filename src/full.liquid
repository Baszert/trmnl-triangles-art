<script>
  run({
    outputWidth: {{ deviceWidth }},
    outputHeight: {{ deviceHeight }},
    columns: {{ columns }},
    strokeWidth: {{ strokeWidth }},
    borderWidth: {{ borderWidth }},
    emptyBias: {{ emptyness }}
  });
</script>